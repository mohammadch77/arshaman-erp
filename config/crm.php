<?php

return [

    /*
     * آستانه‌های بخش‌بندی RFM. موقتی‌اند — طبق هشدار Session رفرنس، دقتشان
     * وقتی سفارش‌های واقعی (فاز ۳) جای تعاملات دستی purchase را بگیرند بالا
     * می‌رود؛ تا آن زمان نیازمند تأیید کسب‌وکاری کارفرمایند.
     */
    'rfm' => [
        // بعد از این تعداد روز از آخرین خرید، مشتری «در معرض ریزش» می‌شود
        'at_risk_days' => 60,

        // بعد از این تعداد روز از آخرین خرید، مشتری «غیرفعال» می‌شود
        'dormant_days' => 180,

        // حداقل تعداد خرید لازم برای دسته «ویژه» (به‌شرط رعایت تازگی هم)
        'vip_min_frequency' => 3,
    ],

];
